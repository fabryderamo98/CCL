@model IEnumerable<CCL.Models.TipoActivo>

@{
    ViewBag.Title = "Index";
}

<h2>Tipo de Activo</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.activo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.activo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idTipoactivo }) |
            @Html.ActionLink("Details", "Details", new { id=item.idTipoactivo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idTipoactivo })
        </td>
    </tr>
}

</table>
<div>
    <td>
        @Html.ActionLink("Descargar Reporte de tipos de Activos", "Print", "TipoActivo")
    </td>

</div>
